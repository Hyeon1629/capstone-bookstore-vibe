# README_TEST.md — 수동 시연 시나리오

본 문서는 각 STEP 종료 시 사용자가 직접 클릭해서 확인할 수동 테스트 시나리오를 누적한다.

## 사전 준비

### Firebase 콘솔 (이미 완료된 항목 외)

1. **Authentication → Sign-in method → Email/Password 활성화**
2. **Firestore Database 생성**
   - 위치: `asia-northeast3` (서울)
   - 시작은 **테스트 모드** (30일간 모든 read/write 허용)
3. 테스트 모드 종료 후엔 보안 규칙 적용. 학교 시연 종료까지는 테스트 모드 유지 가정.

### 개발 서버

```bash
npm run dev
# → http://localhost:5173
```

---

## STEP 2 — 인증 + 온보딩

### 시나리오 A. 신규 회원가입

1. 시크릿 창에서 `http://localhost:5173` 진입
2. 자동으로 `/onboarding` 으로 이동 (3개 슬라이드)
3. 인디케이터 도트 클릭으로 슬라이드 이동, [다음] 버튼으로 진행
4. 마지막 슬라이드 [시작하기] → `/signup` 이동
5. 이메일 입력 → 실시간 형식 검증 (OK / Error)
6. 비밀번호 입력 → 6자 미만 시 카운터 (`3/6`), 6자 이상 시 OK
7. 닉네임 입력:
   - 1자: 형식 오류
   - `서연` 입력 → "확인 중..." → "사용 가능한 닉네임이에요" (300ms 후)
8. [가입하고 시작하기] 버튼 활성화 → 클릭
9. OS 위치 권한 모달 표시 (허용/거부 선택)
10. `/map` placeholder 진입 → UID / email / 닉네임 / fallback area 표시 확인

### 시나리오 B. 닉네임 중복 검증

1. 시나리오 A 로 `서연` 가입 후 로그아웃 (`/map` 의 로그아웃 버튼)
2. 시크릿 창 새로 띄워 다시 `/signup` 진입
3. 동일 닉네임 `서연` 입력 → 300ms 후 "이미 사용 중이에요" + 대안 칩 (`서연_2`, `서연1`, `서연a`)
4. 대안 칩 클릭 시 입력값 자동 교체

### 시나리오 C. 로그인 + 자동 로그인

1. 가입한 계정으로 로그아웃 후 `/login` 진입
2. 잘못된 비밀번호로 시도 → "이메일 또는 비밀번호가 일치하지 않아요"
3. 올바른 비밀번호로 로그인 성공 → `/map`
4. 브라우저 닫고 다시 `http://localhost:5173` 진입
5. **자동 로그인되어 `/map` 으로 바로 이동 확인** (`onAuthStateChanged` + browserLocalPersistence)

### 시나리오 D. 온보딩 1회 노출

1. 시나리오 A·C 마치면 `localStorage.onboardingSeen = 'true'` 설정됨
2. 로그아웃 후 `http://localhost:5173` 진입
3. `/onboarding` 이 아닌 **`/login`** 으로 바로 이동 확인
4. (재현하려면 DevTools → Application → Local Storage → 해당 키 삭제)

### 시나리오 E. 약관·비밀번호 찾기 링크

1. 회원가입 화면의 [이용약관] / [개인정보처리방침] 클릭
2. "v1에서 제공 예정" alert 표시
3. 로그인 화면의 [비밀번호 찾기] 클릭 → 동일 alert

### 시나리오 F. 라우트 가드

1. 로그아웃 상태에서 `http://localhost:5173/map` 직접 진입
2. 자동으로 `/login` 으로 redirect
3. 로그인 상태에서 `http://localhost:5173/login` 직접 진입
4. (현재는 `/login` 페이지 그대로 표시 — STEP 3 에서 가드 강화 검토)

---

## STEP 3 — 지도 홈

### 사전 조건
- 카카오 개발자 콘솔 → 플랫폼 → Web 사이트 도메인에 `http://localhost:5173` 등록됨

### 시나리오 G. 17개 핀 + 카테고리 토글

1. `/map` 진입 시 카카오 지도 라이트 톤으로 로드
2. 17개 핀 모두 표시 (마포 8 + 성동 5 + 종로 4) — 핀은 SVG drop shape + 카테고리 색 + 글리프(book/cup)
3. 헤더에 "반경 2km 안에 책방 17곳" (lamp 색 강조)
4. 카테고리 칩 [서점·헌책방] 탭 → 주황 핀만 남고 초록 핀은 숨김
5. [도서관·북카페] 탭 → 초록 핀만
6. [전체 · 17] 탭 → 전체 복원

### 시나리오 H. 검색 → 마포구립서강도서관·서강대 로욜라 강조 ⭐ (시연 핵심)

1. 헤더 검색바에 "서강" 입력
2. 매칭 외 핀은 opacity 0.3 으로 dim
3. **마포구립서강도서관** 과 **서강대학교 로욜라도서관** 만 선명하게 남음
4. 검색 첫 결과의 좌표로 자동 setCenter
5. 검색바 우측 ✕ 버튼으로 초기화

### 시나리오 I. 핀 탭 → 미리보기 시트 → 상세

1. 마포구립서강도서관 핀 탭
2. 핀 focused 상태 (글로우 + scale 1.08 + translateY -4px)
3. 하단 미리보기 시트가 slide-up (250ms, translateY 20→0)
4. 시트에 책방명 / 카테고리 / 영업 중 / 거리 표시
5. 시트 [상세 →] 클릭 → `/bookstore/mapo-seogang-lib` placeholder 페이지로 이동 ("STEP 4 예고")
6. 뒤로가기 → 지도 홈 유지

### 시나리오 J. 검색 0건

1. 검색바에 "xyz_없음" 입력
2. 모든 핀이 dim 처리
3. 지도 중앙에 "근처에 일치하는 책방이 없어요" floating 메시지

### 시나리오 K. 현재 위치 FAB

1. 사용자 위치 권한 허용 후 진입 시 `/map` 의 setUserLocation 자동 호출
2. 핀 시트 미오픈 상태에서 FAB 위치 bottom 24px
3. 시트 열린 상태에서 FAB 위치 bottom 168px (250ms 트랜지션)
4. FAB 클릭 시 사용자 좌표(또는 fallback 마포구립서강도서관) 로 setCenter

### 시나리오 L. BottomNav 탭 전환

1. 하단 [북쉘프] 탭 → `/bookshelf` placeholder (STEP 6 예고)
2. 하단 [마이] 탭 → `/mypage` placeholder + 로그아웃 버튼
3. [지도] 탭 → `/map` 복귀
4. 활성 탭의 글리프는 filled, 비활성은 outline

---

## STEP 4 — 책방 상세

### 시나리오 M. 책방 상세 진입 + 영업 상태

1. `/map` 에서 핀 탭 → 시트 [상세 →] → `/bookstore/{id}` 진입
2. 상단 사진 슬라이드 (320px) + 도트 인디케이터 + 우하단 `1 / 3` 카운터
3. 좌상단 [뒤로] / 우상단 [북마크] (clear→fill 토글)
4. 책방명 H1 + 카테고리 dot · uppercase + 영업 상태:
   - 현재 시각이 영업 시간 내면 `● 영업 중`
   - 종료 30분 이내면 `● 곧 종료 · N분`
   - 그 외 `● 영업 종료`
5. 오늘 영업시간 표시

### 시나리오 N. 분위기 태그 집계 (시드 + Firestore 머지) ⭐

1. **마포구립서강도서관** (`/bookstore/mapo-seogang-lib`) 진입
2. 분위기 섹션에 `🤫 한적한 9` + `☕ 독서하기 좋은 날 3` 표시 (seedMoods 우선)
3. **서강대 로욜라도서관** 진입 시 `🤫 한적한 14` + `🌧️ 비오는 날 2`
4. 시드 없는 책방 (예: 반포 책방) 진입 시 빈 상태 5개 ghost emoji 표시
5. STEP 5 이후 Firestore moodTags 가 쌓이면 시드와 자동 합산 (검증은 STEP 5 시나리오에서)

### 시나리오 O. 외부 액션 — 전화 / 길찾기 / 공유

1. [전화] 클릭 → tel: 인텐트 (마포구립서강도서관 02-3141-7053)
2. 전화번호 없는 책방은 [전화] disabled
3. [길찾기] 클릭 → 새 탭에 카카오맵 길찾기 URL 열림
4. [공유] 클릭 → localhost 환경에선 클립보드 복사 + "링크가 복사되었어요" 토스트
5. (실제 모바일 시연 환경에서 Web Share API 가 가능하면 OS 공유 시트 표출)

### 시나리오 P. 영업 상태 자가 검증 스크립트

```bash
npx tsx src/lib/businessHours.demo.ts
```
- 5/5 PASS 확인 (open / closed / closing-soon / 주말 분리 4번째 / 주말 분리 5번째)

### 시나리오 Q. 404

1. `/bookstore/없는-id` 직접 진입
2. "책방을 찾을 수 없어요" + [지도로 돌아가기]

---

## STEP 5 — GPS 인증 + 분위기 입력 + 시연 모드

### 사전 조건

**Firestore 인덱스 생성** (첫 인증 시도 시 콘솔이 안내):

1. `/map` 에서 시연 모드 ON → 핀 long-press → 첫 인증 시도
2. 콘솔(Devtools) 에 `The query requires an index` 에러 + 자동 생성 링크 출력
3. 링크 클릭 → Firebase 콘솔에서 [생성] 클릭 → 1~2분 대기
4. 인덱스 활성화 후 다시 시도

(또는 시연 직전 미리 Firebase Console → Firestore → 색인 → `visits` 컬렉션 + `userId Asc, bookstoreId Asc, visitedAt Desc` 복합 색인 수동 추가)

### 시나리오 R. 시연 모드 ON + 즉시 인증 ⭐ (발표 시연 8~9번)

1. `/mypage` 진입 → 시연 모드 토글 ON
2. 지도(`/map`) 복귀 → 우상단에 **DEMO · 시연 모드** 배지 노출
3. 마포구립서강도서관 핀을 **0.5초 길게 누름** (long-press)
4. 화면에 **radar pulse** 풀스크린 2초간 표시 (info 컬러 3개 ring 확장)
5. **paper fadein** 250ms (방문 확인 신호)
6. **MoodInputModal** 진입: 황금 스탬프(점선 회전 ring 12s) + 7개 스파클 + `STAMP COLLECTED · #001`
7. 5개 이모지 중 `🤫 한적한` 선택 → 카드 lamp 보더 + `translateY -2px`
8. [북쉘프에 기록하기] 클릭 → Firestore visits + moodTags 추가 → `/bookshelf` 이동

### 시나리오 S. 24시간 중복 방지

1. 시나리오 R 직후, 다시 `/map` 으로 와서 같은 마포구립서강도서관 핀을 long-press
2. radar pulse 없이 상단 토스트 "이미 방문 인증한 책방이에요" 2.5초 표시
3. 모달 표시 안 됨

### 시나리오 T. 방문 기록 초기화 (시연 반복용)

1. `/mypage` 의 시연 모드 카드 → [방문 기록 초기화] 클릭
2. confirm 다이얼로그 → 확인
3. visits + moodTags 모두 삭제 → "N개 기록을 삭제했어요" 토스트
4. 다시 `/map` 으로 와서 같은 핀 long-press → 정상 인증 가능

### 시나리오 U. 시연 모드 OFF + 일반 GPS

1. `/mypage` 시연 모드 OFF
2. `/map` 우상단 배지 사라짐, 핀 long-press 는 일반 탭과 동일하게 시트만 열림
3. 실 GPS 가 책방 50m 이내 + 5초 체류 시에만 자동 트리거 (실 모바일 시연 환경)
4. 데스크탑 브라우저에서는 실 GPS 가 책방과 멀리 떨어져 있으니 자동 트리거 안 됨 — **시연 모드 ON 필수**

### 시나리오 V. 분위기 모달 닫기

1. 시나리오 R 의 모달 표시 상태에서 키보드 `ESC` 눌러 닫기
2. visits/moodTags 모두 저장 안 됨 (트리거 취소 효과)
3. 배경 클릭은 무시 (5 이모지 선택 유도)
4. [건너뛰기] 클릭 시 — visits 만 저장되고 moodTag 는 저장 안 됨 → `/bookshelf` 이동

---

## STEP 6 — 마이 북쉘프 + 마이페이지

### 시나리오 W. 빈 북쉘프

1. 신규 가입 직후 또는 마이페이지 [방문 기록 초기화] 후 `/bookshelf` 진입
2. 빈 책장 SVG (3단 책장 + 점선 빈 책 자리 + 펜던트 글로우)
3. "첫 발견을 기다리는 책장이에요" + [지도에서 발견하기] Primary 버튼
4. 버튼 클릭 → `/map`

### 시나리오 X. 누적 통계 + 카운트업 ⭐

1. 시연 모드로 책방 2~3곳 인증
2. `/bookshelf` 진입 → 누적 통계 카드의 숫자가 0 → 실제값까지 약 0.8초 카운트업
3. 좌측 누적 방문 (lamp 색 강조), 우측 이번 달 (paper 색)
4. 카드 상단에 누런 띠 1px 보일 듯 말 듯 (책 등받이 메타포)

### 시나리오 Y. 방문 지도 미니뷰 + 책 등 리스트

1. `/bookshelf` 의 방문 지도 SVG: 방문 책방은 컬러 핀(드롭쉐도우 글로우), 미방문은 회색 점
2. 좌하단 legend: `● 방문 N` + `● 미방문 N`
3. 아래 방문 기록: 책 등 spine 모티프 (36×44, NO.01, NO.02 ... Gowun Batang)
4. 분위기 이모지가 있으면 책방명 옆에 표시 (시연 모드로 인증 시 선택한 이모지)
5. 리스트 항목 staggered fade-in (40ms 간격)
6. 항목 클릭 → `/bookstore/:id` 상세

### 시나리오 Z. 마이페이지 프로필 카드

1. `/mypage` 진입
2. 프로필 카드: 아바타(닉네임 첫 글자 + lamp 그라데이션 + glow) + 닉네임 + 이메일 + `★ EXPLORER · LV.N`
3. 누적 방문 3개 미만 → `START`, 1~6개 → `LV.1`, 7~12개 → `LV.2`, 13개 이상 → `LV.3`
4. 하단 MiniStat 2열: 누적 방문 / 이번 달 (카운트업)
5. 시연 모드 토글 카드 (STEP 5)
6. 메뉴 그룹: [설정] (알림·앱 정보 - 클릭 시 토스트) / [계정] (로그아웃)
7. 푸터: `숨은 책방 · v1.0.0`

### 시나리오 AA. 로그아웃 다이얼로그

1. `/mypage` [로그아웃] 메뉴 (error 색) 클릭
2. 풀스크린 scrim + 중앙 카드 모달 (`로그아웃 하시겠어요?`)
3. ESC / 배경 클릭 / [취소] 클릭 모두 모달 닫기
4. [로그아웃] 클릭 → Firebase signOut → `/login` 으로 redirect

---

## 카카오 실 데이터 통합 (보조)

### 시나리오 BB. 실시간 책방·도서관 검색 ⭐

1. `/map` 진입 시 시드 17곳 표시 + 카카오 API 가 현재 지도 영역의 추가 책방을 백그라운드 검색
2. 헤더 카피가 `책방 17곳` 에서 `책방 N곳 · 실시간 +M` 으로 갱신
3. 지도 위쪽 중앙에 작은 인디케이터 노출:
   - 검색 중: lamp 펄스 + "실시간 검색 중..."
   - 완료: ok dot + "실시간 N곳 추가"
   - 실패: error dot + "카카오 검색 실패"
4. 추가된 핀은 시드와 동일한 SVG 디자인 (카테고리별 색)

### 시나리오 CC. 지도 패닝 시 자동 재검색

1. 지도를 다른 동(예: 합정에서 종로) 으로 패닝
2. 0.5초 후 자동으로 카카오 API 재호출
3. 새 영역의 추가 책방이 합쳐서 표시 (이전 핀도 유지)
4. 시드 좌표 80m 이내의 중복 책방은 시드만 표시되고 카카오 결과는 dedupe

### 시나리오 DD. 카카오 책방 상세

1. 카카오에서 추가된 핀 탭 → 시트 → [상세 →]
2. `/bookstore/kakao-{place_id}` 로 라우팅
3. 책방명·주소·전화 (있으면) 정상 표시
4. 영업시간은 "운영시간 정보 없음" 표시
5. 분위기 태그는 빈 상태 (시드 mood 없음 → "첫 번째 분위기를 남겨주세요" + 5 이모지 ghost)
6. [전화] / [길찾기] / [공유] 액션 모두 작동
7. **시연 모드 ON + 핀 long-press 도 동작** (24h 중복 검증은 카카오 ID `kakao-{place_id}` 기반)
